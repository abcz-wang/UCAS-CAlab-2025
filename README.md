# UCAS-CALab-2025

中国科学院大学 CALab 项目 — FPGA 箱号 13

---

## 项目说明

本项目是 CALab 2025 FPGA 实验系列的一部分，注意每做一个新项目，除了写注释，还务必要写README，交代你干了什么，新加文件的各个信号的作用等等，还可以摘录讲义重要的话，不要嫌多，为了后人更方便

---

## 任务信息

### Task12
- 注意：CSR 指令运行在特权态。
- 执行 CSR 指令时，CRMD 的 PLV 位必须为 `00`，否则会触发 **指令特权级错例外**。
- 当前版本尚未实现该特性，需要在后续版本中完善。

### Task13
- `inst_rdcntid` 指令必须视为 CSR 特权指令，否则仿真无法通过。

### Task14
- 延迟为0x003F的时候，上板时不时卡住，仿真未发现问题，在task16，此问题消失
- 修改延迟的方法为修改soc_verify\soc_axi\rtl\CONFREG\confreg.v的`define RANDOM_SEED {7'b1010101,16'h00FF}中的00FF，板子上8个开关，每个开关对应上面两个指示灯，从前往后一共16个灯，每个灯亮表示这一位为1，全亮了就是0b1111111111111111，后六个灯亮就是0b0000000000111111（0x3F）

### Task16
- 详见注释，实现类sram接口和axi转接桥，可在随机延迟下运行

### Task20

- 首先我们来明确一下与 Cache 模块相关的主要设计规格，避免因为后续的讨论过于
宏观而无法具体到细节。这些设计规格包括以下方面：
1）CPU 内部集成一个指令 Cache 和一个数据 Cache。 2）指令 Cache 和数据 Cache 的容量均为 8KB，均为两路组相联，Cache 行大小均
为 16B。 3）指令 Cache 和数据 Cache 采用 Tag 和 Data 同步访问的形式。
4）指令 Cache 和数据 Cache 均采用“虚 Index 实 Tag”（VIPT）的访问形式。
5）指令 Cache 和数据 Cache 均采用伪随机替换算法。
6）数据 Cache 采用写回写分配的策略。
7）指令 Cache 和数据 Cache 均采用阻塞式（Blocking）设计，即一旦发生 Cache 
Miss（未命中，也称“缺失”），则阻塞后续访问直至数据填回 Cache 中。
8）Cache 不采用“关键字优先”技术。

- 对于cache与CPU的通路在表10.2定义。
- 
- 对于Cache与转接桥的通路在P248表10.3定义，同时：上述接口定义好之后，大家还要对现有的类 SRAM-AXI 总线转接桥模块进行相应调整。请注意，此时该模块对内是两组读接口、一组写接口，第一组是指令 Cache 的
rd_* 和 ret_*，第二组是数据 Cache 的 rd_* 和 ret_*，第三组是数据 Cache 的 wr_*。虽
然接口看上去多了一组，但整个转接桥内部的数据通路并不需要进行大的调整。

- 关于代码，两个hit write都是阻塞处理，将信号addr_ok置0来阻塞流水线一拍。生成了两个IP核，注意后面项目也要生成。
---

## 使用方法
-我生成了一个脚本 `run.sh`，可以直接生成作业提交的文件，详见run.sh的注释或下面用法

### 脚本用法

```bash
./run.sh <prj号> <上一个任务名> <当前任务名>
示例: ./run.sh 5 exp15 exp16
```
需要将该脚本放在任务文件夹下，如/exp13下
且要改目录
